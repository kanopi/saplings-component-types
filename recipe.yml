name: 'Saplings - Component types'
description: 'Configuration for Saplings component types.'
type: 'Site'
install:
  # Core.
  - block_content
  - media
  # Contrib.
  - block_field
  - ds
  - nomarkup
  - viewsreference
config:
  import:
    # Core.
    # module_name:
    #  - one.config.name
    # Contrib.
    # block_field
